import type { Metadata } from "next";
import { Panel } from "@/components/ui/card";
import { ConnectRepositoryForm } from "@/features/repositories/connect-repository-form";

export const metadata: Metadata = { title: "Connect repository" };
export default function ConnectRepositoryPage() {
  return <div className="layout-standard layout-gutter py-10 max-[820px]:py-8"><p className="type-section-eyebrow text-muted-foreground">Repository onboarding</p><h1 className="mt-2 type-page-title">Connect <span className="italic text-primary">repository</span><span className="not-italic">.</span></h1><p className="mt-2 max-w-[68ch] type-body text-text-secondary">Provide a GitHub repository URL. Giro submits it to the configured backend, then opens the existing indexing progress flow.</p><div className="mt-7 grid items-start gap-8 laptop:grid-cols-[minmax(0,1fr)_300px]"><Panel className="border border-border-subtle"><div><h2 className="type-panel-title">Repository source</h2><p className="mt-1 type-compact text-muted-foreground">Accepted format: <span className="type-mono text-foreground">https://github.com/owner/repository</span></p></div><ConnectRepositoryForm /></Panel><aside aria-labelledby="connection-process-heading" className="border-y border-border-subtle py-4"><h2 id="connection-process-heading" className="type-metadata-label text-muted-foreground">What happens after submission</h2><ol className="mt-3 divide-y divide-border-subtle">{["The backend checks whether a healthy index already exists.", "New work is queued for the supported clone and indexing pipeline.", "Giro opens live indexing status and reports backend stage updates."].map((item, index) => <li key={item} className="flex gap-3 py-3"><span className="type-metadata text-primary">0{index + 1}</span><span className="type-compact text-text-secondary">{item}</span></li>)}</ol><p className="mt-4 type-compact text-muted-foreground">Repository access is controlled by GitHub credentials configured in the Giro backend. This form does not grant or change GitHub permissions.</p><p className="mt-3 type-compact text-muted-foreground">Indexing runs asynchronously. Leaving the progress screen does not cancel the backend job.</p></aside></div></div>;
}
